{
  "name": "node-intro",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "tsc && node dist/main.js",
    "build-and-run:native": "tsc && concurrently --kill-others \"tsc --watch\" \"nodemon dist/main.js\"",
    "build-and-run:docker": "tsc && concurrently --kill-others \"tsc --watch\" \"nodemon -L --polling-interval 100 dist/main.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.1.0",
    "typescript": "^5.4.3"
  }
}
